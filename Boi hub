-- Boy Hub | Optimized Pro Version
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Remotes = ReplicatedStorage.Remotes.CommF_

getgenv().Settings = {
    AutoFarm = false,
    BringMob = false,
    FastAttack = true
}

-- Improved Targeting System
local function GetClosestEnemy()
    local Closest = nil
    local Distance = math.huge
    for _, Enemy in pairs(workspace.Enemies:GetChildren()) do
        if Enemy:FindFirstChild("HumanoidRootPart") and Enemy:FindFirstChild("Humanoid") and Enemy.Humanoid.Health > 0 then
            local Mag = (Enemy.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
            if Mag < Distance then
                Distance = Mag
                Closest = Enemy
            end
        end
    end
    return Closest
end

-- Core Farming Loop
task.spawn(function()
    while task.wait(0.1) do -- Increased frequency for better response
        if getgenv().Settings.AutoFarm then
            local Target = GetClosestEnemy()
            if Target then
                local MyHRP = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                local TargetHRP = Target:FindFirstChild("HumanoidRootPart")

                if MyHRP and TargetHRP then
                    -- 1. Bring Mob Logic
                    if getgenv().Settings.BringMob then
                        TargetHRP.CFrame = MyHRP.CFrame * CFrame.new(0, 0, 5) -- Stacking mobs in front
                    end

                    -- 2. Position: Stick on top of the mob
                    MyHRP.CFrame = TargetHRP.CFrame * CFrame.new(0, 5, 0)

                    -- 3. Fast Attack
                    if getgenv().Settings.FastAttack then
                        Remotes:InvokeServer("AttackStart")
                        -- Add extra trigger to boost speed if game allows
                        task.spawn(function()
                            Remotes:InvokeServer("AttackStart")
                        end)
                    end
                end
            end
        end
    end
end)

-- Quest Auto-Accept Logic (Simplified)
task.spawn(function()
    while task.wait(2) do
        if getgenv().Settings.AutoFarm then
            if not LocalPlayer.PlayerGui.Main.Quest.Visible then
                -- Note: You should replace this with a dynamic quest finder for your specific level
                Remotes:InvokeServer("StartQuest", "BanditQuest1", 1) 
            end
        end
    end
end)
