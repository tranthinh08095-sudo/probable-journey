getgenv().Config = {AutoFarm = true, BringMob = true, AutoFruit = true} local p = game.Players.LocalPlayer local function active() return getgenv().Config ~= nil end local sg = Instance.new("ScreenGui", game:GetService("CoreGui")) local frame = Instance.new("Frame", sg) frame.Size = UDim2.new(0, 130, 0, 140) frame.Position = UDim2.new(0, 15, 0, 60) frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20) frame.Visible = false local tBtn = Instance.new("TextButton", sg) tBtn.Size = UDim2.new(0, 65, 0, 28) tBtn.Position = UDim2.new(0, 15, 0, 20) tBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40) tBtn.Text = "MENU" tBtn.TextColor3 = Color3.fromRGB(255, 255, 255) tBtn.Font = Enum.Font.SourceSansBold tBtn.TextSize = 14 tBtn.MouseButton1Click:Connect(function() if active() then frame.Visible = not frame.Visible end end) local function create(flag, label, y) local b = Instance.new("TextButton", frame) b.Size = UDim2.new(1, -10, 0, 22) b.Position = UDim2.new(0, 5, 0, y) b.Font = Enum.Font.SourceSans local function refresh() if getgenv().Config and getgenv().Config[flag] then b.Text = label .. ": ON" b.BackgroundColor3 = Color3.fromRGB(34, 139, 34) else b.Text = label .. ": OFF" b.BackgroundColor3 = Color3.fromRGB(139, 0, 0) end end refresh() b.MouseButton1Click:Connect(function() if getgenv().Config then getgenv().Config[flag] = not getgenv().Config[flag] refresh() end end) end create("AutoFarm", "Farm Level", 15) create("BringMob", "Gom Mob", 45) create("AutoFruit", "Snipe Fruit", 75) local ex = Instance.new("TextButton", frame) ex.Size = UDim2.new(1, -10, 0, 22) ex.Position = UDim2.new(0, 5, 0, 105) ex.Text = "EXIT SCRIPT" ex.BackgroundColor3 = Color3.fromRGB(150, 0, 0) ex.TextColor3 = Color3.fromRGB(255, 255, 255) ex.Font = Enum.Font.SourceSansBold ex.MouseButton1Click:Connect(function() getgenv().Config = nil sg:Destroy() end) task.spawn(function() while task.wait(0.1) and active() do if getgenv().Config.AutoFarm and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then pcall(function() for _, e in pairs(game:GetService("Workspace").Enemies:GetChildren()) do if e:FindFirstChild("HumanoidRootPart") and e:FindFirstChild("Humanoid") and e.Humanoid.Health > 0 then while e.Humanoid.Health > 0 and active() and getgenv().Config.AutoFarm do task.wait() p.Character.HumanoidRootPart.CFrame = e.HumanoidRootPart.CFrame * CFrame.new(0, 6, 0) game:GetService("VirtualUser"):CaptureController() game:GetService("VirtualUser"):ClickButton1(Vector2.new(851, 529)) end end end end) end end end) task.spawn(function() while task.wait() and active() do if getgenv().Config.BringMob and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then pcall(function() for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do if v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then if (v.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude <= 300 then v.HumanoidRootPart.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -4) v.HumanoidRootPart.CanCollide = false end end end end) end end end) task.spawn(function() while task.wait(1) and active() do if getgenv().Config.AutoFruit and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then pcall(function() for _, v in pairs(game:GetService("Workspace"):GetChildren()) do if v:IsA("Tool") and string.find(v.Name, "Fruit") then p.Character.HumanoidRootPart.CFrame = v.Handle.CFrame end end end) end end end)
