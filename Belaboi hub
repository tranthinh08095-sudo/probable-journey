getgenv().Config = {AutoFarm = true, BringMob = true, Weapon = "Melee"} local p = game.Players.LocalPlayer local function active() return getgenv().Config ~= nil end local sg = Instance.new("ScreenGui", game:GetService("CoreGui")) local frame = Instance.new("Frame", sg) frame.Size, frame.Position, frame.BackgroundColor3, frame.Visible = UDim2.new(0, 130, 0, 160), UDim2.new(0, 15, 0, 60), Color3.fromRGB(20, 20, 20), false local tBtn = Instance.new("TextButton", sg) tBtn.Size, tBtn.Position, tBtn.BackgroundColor3, tBtn.Text, tBtn.TextColor3, tBtn.Font, tBtn.TextSize = UDim2.new(0, 65, 0, 28), UDim2.new(0, 15, 0, 20), Color3.fromRGB(40, 40, 40), "MENU", Color3.fromRGB(255, 255, 255), Enum.Font.SourceSansBold, 14 tBtn.MouseButton1Click:Connect(function() if active() then frame.Visible = not frame.Visible end end) local title = Instance.new("TextLabel", frame) title.Size, title.Position, title.BackgroundColor3, title.Text, title.TextColor3, title.Font, title.TextSize = UDim2.new(1, 0, 0, 20), UDim2.new(0, 0, 0, 0), Color3.fromRGB(35, 35, 35), "BOY HUB", Color3.fromRGB(255, 255, 255), Enum.Font.SourceSansBold, 12 local wBtn = Instance.new("TextButton", frame) wBtn.Size, wBtn.Position, wBtn.BackgroundColor3, wBtn.TextColor3, wBtn.Text, wBtn.Font, wBtn.TextSize = UDim2.new(1, -10, 0, 22), UDim2.new(0, 5, 0, 25), Color3.fromRGB(70, 130, 180), Color3.fromRGB(255, 255, 255), "Weapon: Melee", Enum.Font.SourceSansBold, 12 wBtn.MouseButton1Click:Connect(function() if getgenv().Config then getgenv().Config.Weapon = getgenv().Config.Weapon == "Melee" and "Sword" or getgenv().Config.Weapon == "Sword" and "Gun" or "Melee" wBtn.Text = "Weapon: " .. getgenv().Config.Weapon end end) local function create(flag, label, y) local b = Instance.new("TextButton", frame) b.Size, b.Position, b.Font = UDim2.new(1, -10, 0, 22), UDim2.new(0, 5, 0, y), Enum.Font.SourceSans local function refresh() if getgenv().Config and getgenv().Config[flag] then b.Text, b.BackgroundColor3 = label .. ": ON", Color3.fromRGB(34, 139, 34) else b.Text, b.BackgroundColor3 = label .. ": OFF", Color3.fromRGB(139, 0, 0) end end refresh() b.MouseButton1Click:Connect(function() if getgenv().Config then getgenv().Config[flag] = not getgenv().Config[flag] refresh() end end) end create("AutoFarm", "Farm Level", 55) create("BringMob", "Gom Mob", 85) local ex = Instance.new("TextButton", frame) ex.Size, ex.Position, ex.Text, ex.BackgroundColor3, ex.TextColor3, ex.Font = UDim2.new(1, -10, 0, 22), UDim2.new(0, 5, 0, 125), "EXIT SCRIPT", Color3.fromRGB(150, 0, 0), Color3.fromRGB(255, 255, 255), Enum.Font.SourceSansBold ex.MouseButton1Click:Connect(function() getgenv().Config = nil sg:Destroy() end) local function equip() pcall(function() local char = p.Character if char then for _, v in pairs(p.Backpack:GetChildren()) do if v:IsA("Tool") and v.ToolTip == getgenv().Config.Weapon then char.Humanoid:EquipTool(v) end end end end) end task.spawn(function() while task.wait(0.05) and active() do if getgenv().Config.AutoFarm and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then pcall(function() for _, e in pairs(game:GetService("Workspace").Enemies:GetChildren()) do if e:FindFirstChild("HumanoidRootPart") and e:FindFirstChild("Humanoid") and e.Humanoid.Health > 0 then while e.Humanoid.Health > 0 and active() and getgenv().Config.AutoFarm do task.wait() equip() p.Character.HumanoidRootPart.CFrame = e.HumanoidRootPart.CFrame * CFrame.new(0, 4, 0) game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AttackStart") game:GetService("VirtualUser"):CaptureController() game:GetService("VirtualUser"):ClickButton1(Vector2.new(851, 529)) end end end end) end end end) task.spawn(function() while task.wait() and active() do if getgenv().Config.BringMob and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then pcall(function() for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do if v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then if (v.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude <= 300 then v.HumanoidRootPart.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame.new(0, -4, -1) v.HumanoidRootPart.CanCollide = false end end end end) end end end)
